name: Class Reminder Notifications

on:
  schedule:
    - cron: "* * * * *"
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Call notify endpoint
        run: curl -s https://dash-ashen-kappa.vercel.app/api/notify